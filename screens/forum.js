import * as React from 'react';

import {
    SafeAreaView,
    View,
    Text,
    Image,
    FlatList,
    TouchableOpacity
} from "react-native"
import { COLORS, SIZES, FONTS, icons, images } from "../constants"

const Forum = () => {
    return (
        <View>
              <Text>Forum</Text>
        </View>
    )
}

export default Forum